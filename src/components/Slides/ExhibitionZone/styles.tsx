import styled from "styled-components";
import { COLORS } from "shared/colors";

export const Wrapper = styled.div`
  height: 100%;
`;

export const EventTitle = styled.div`
  color: ${COLORS.orange};
`;

export const EventSubDescription = styled.div`
  margin-top: 16px;
  color: white;
  font-style: normal;
  font-weight: 500;
`;
